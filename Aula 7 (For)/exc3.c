#include <stdio.h>

int main() {
    float base, resultado = 1;
    int expoente, num;

    for(base = 0; num < expoente; num++) {
        resultado *=base;
    }

    printf("Resultado: %.2f\n", resultado);

    return 0;
}